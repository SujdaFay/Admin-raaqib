import 'package:flutter/material.dart';

class AbealsPage extends StatelessWidget {
  const AbealsPage({super.key});

  @override
  Widget build(BuildContext context) {
     return const Center(child: Text("Apeal page"));
  }
}