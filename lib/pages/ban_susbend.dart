import 'package:flutter/material.dart';

class BanSusbend extends StatelessWidget {
  const BanSusbend({super.key});

  @override
  Widget build(BuildContext context) {
     return const Center(child: Text("Ban and susbend"));
  }
}